<?php

    $arr = array('html','css','javascript','python',2,12,'2');

    // 通过sort可以对数组进行排序
    // 正序(升序)       索引值也重新排序，不保留原来索引值
    // sort($arr);

    // 逆序(降序)       索引值也重新排序，不保留原来索引值
    // rsort($arr);
    // print_r($arr);

    // 正序排列     保留原来索引值
    // asort($arr);
    // print_r($arr);
    // 逆序(降序)   保留原来索引值
    // arsort($arr);   
    // 排序是在原数组直接操作
    // print_r($arr);

    // $user = array('name'=>'陈瑶','age'=>18);

    // 根据关联数组的 key 进行排序 正序
    // ksort($user);
    // 倒序
    // krsort($user);